import * as React from "react";
import NavBar from "./view-settings/nav/NavBar";

const Settings = (): React.ReactElement => {
  return (
    <div>
      <NavBar />
    </div>
  );
};

export default Settings;
